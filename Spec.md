# 个人知识助手 · 作品 Spec

> 30 天 AI 学习周期最终作品设计文档（t22 产出，2026-09-05）
> 技术栈：DeepSeek + LangGraph + Chroma + MCP（全 Python）

## 一、功能列表（用户视角）

1. 知识问答：问知识类问题，助手查资料后回答，并说明答案依据
2. 工具能力：问计算、查询类问题，助手自动调用工具执行
3. 拒答保护：资料库之外的问题不乱编，明确说不会
4. 跨对话记忆：记住之前聊过的内容和偏好
5. 长对话稳定：聊得再长也不忘开头、不卡顿

（内部实现手段——ReAct 循环、滑动窗口、摘要压缩、Checkpointer——不进入功能列表）

## 二、架构图（部件清单）

| 部件 | 职责 | 施工课次 |
|---|---|---|
| 门面（对话界面） | 用户输入/输出；命令行先行，前端 t26 | t23 命令行 / t26 前端 |
| 图（LangGraph） | 总指挥：编排「推理→决策→分岔→回填→循环」的 ReAct 闭环 | t23 |
| 大脑（DeepSeek） | 决策：是否查资料/调工具；组织最终回答 | t23 |
| 资料库管道（RAG） | 本地文件→分块→向量化→Chroma→检索召回 top_k | t24 |
| 工具箱（MCP） | Server 端工具（add/pet_care）真正执行；Client 做信使传请求带结果 | t23 基础 / t26 扩展 |
| 记事本（记忆） | 会话消息列表 + Checkpointer 落盘 + 长期记忆文件 | t23 Checkpointer / t25 长期记忆 |
| 成本控制（实现层） | 滑动窗口 / 摘要，防止上下文爆炸 | t25 |

**连接关系**：门面 → 图 → 模型节点 ⇄（条件边分岔）检索节点 / 工具节点 → 回填模型 → 循环至 END → 门面。Checkpointer 挂在图上，每个节点跑完存档。

## 三、数据流（一次完整旅程）

用户输入 → 门面交给图 → 图从 Checkpointer 按 thread_id 取出历史对话 → 组装上下文送进模型节点 → 模型决策：

- 需要查资料 → 图走检索节点（RAG 管道查 Chroma）→ 结果回填 → 模型再决策
- 需要调工具 → 图走工具节点（Client 传请求给 MCP Server 执行，结果带回）→ 回填 → 模型再决策
- 不需要 → 直接 END（含拒答场景：资料外问题答「这个我不会」）

循环直到模型认为信息足够 → 组织最终回答 → 图结束 → 结果返回门面；全程每节点存档（Checkpointer 落盘，跨对话可恢复）。

## 四、验收标准

1. 应用结果：用户输入问题，系统端到端完成工具调用 / 资料检索 / 回答输出，五个功能全部可演示
2. 知识结果：能向面试官完整解释链路，说清每个部件的原理与选型理由
3. 作品结果：代码上传 GitHub，本地一键运行；README 完整，演示脚本 5 分钟走完
4. 时间边界：核心功能完善、基础 bug 修复、文档齐全后，09-13 前完成最终验收（D28 标准自评）

## 五、施工顺序（t23-t27）

t23 骨架 + 对话 + 工具调用 → t24 RAG 接入 → t25 记忆模块 → t26 MCP 扩展 + 前端 → t27 打磨（错误处理/测试/README）→ t28 最终验收 → t29/t30 面试冲刺
